var ctx = document.getElementById("game").getContext("2d"); //get the context of the canvas
function runFrame(){
    
}

var interval = setInterval(runFrame,1000/60); // run the function 'runFrame' every 60th of a second